services:
  - type: web
    name: content-automation
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    plan: free

envVars:
  - key: PYTHON_VERSION
    value: 3.10.13
